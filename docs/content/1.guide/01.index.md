---
title: Getting Started
icon: i-solar-book-2-linear
description: Install, encode one line, decode it back. Same shape from every cipher.
---

::warning
**Pre-1.0.** API and tool list can still move. Pin exact versions if you build on it now.
::

::caution
**Nothing here protects a secret.** Lessons, riddles, CTFs - yes. Secrets - no. Anyone with a laptop breaks the classical ciphers in seconds, AES and Triple DES run mostly in ECB, which shows which blocks repeat, and the code was never made constant time. Use a vetted library for anything that matters.
::

## Why this exists

A puzzle says "the key is LEMON" and I want the plaintext, not a lecture. An agent gets a ROT-47 string in a challenge and needs to flip it without leaving the process. Each cipher is small, but every one of them has conventions that decide whether your answer matches the author's: does Playfair fold J into I, does Bacon use 24 or 26 letters, which rotors is "Enigma" even. Every online tool picks something different and rarely says what.

So `@agntn/ciphers` picks once, writes it down, and gives every cipher the same two methods. No HTTP, no keys, no telemetry. It runs in your process and that's it.

## Install

```bash
pnpm add @agntn/ciphers
```

## First call

```ts [vigenere.ts]
import { create } from "@agntn/ciphers";

const vigenere = create("vigenere");

const encoded = vigenere.encode("ATTACK AT DAWN", { key: "LEMON" });
console.log(encoded.text); // LXFOPV EF RNHR

const decoded = vigenere.decode(encoded.text, { key: "LEMON" });
console.log(decoded.text); // ATTACK AT DAWN
```

The package ships thirty-nine ciphers from one list. `create(name)` gives you one cached instance and wants the exact registry name. `resolveCipher("Rail Fence")` lowercases, turns spaces into hyphens, and then also wants an exact match. No prefixes, no fuzzy matching, a typo is an `UnknownCipherError`. Guessing which cipher you meant is how you get the wrong plaintext with a straight face.

## The shape

Every `encode` and `decode` returns this:

```ts
interface CipherResult {
  text: string; // the output
  cipher: string; // "vigenere"
  operation: "encode" | "decode";
  options: Record<string, unknown>; // what was actually applied, defaults included
  normalizedInput?: string; // optional, for a cipher that reshapes its input; the built-ins leave it out
}
```

Two options every `classical` cipher understands: `preserveCase` (default `true`) keeps lowercase letters lowercase, `stripNonAlpha` (default `false`) drops everything that is not a letter before the cipher runs. The rest is per cipher - `shift`, `key`, `rails`, `a`, `b`, `period`, `letters`, `positions`, `rings`, `plugboard`, `iv`, `segment`, `blockSize`, `nonce`, `aad`, `tagLength`, `tweak`. Each cipher's page lists its own, and so does `create(name).info()`.

## What ships

| Cipher | Family | Options | Self-inverse |
| --- | --- | --- | :---: |
| [caesar](/ciphers/caesar) | shift | `shift` (1 to 25, default 3) | ✗ |
| [rot13](/ciphers/rot13) | shift | | ✓ |
| [rot47](/ciphers/rot47) | shift | | ✓ |
| [atbash](/ciphers/atbash) | reflection | | ✓ |
| [vigenere](/ciphers/vigenere) | polyalphabetic | `key` | ✗ |
| [beaufort](/ciphers/beaufort) | polyalphabetic | `key` | ✓ |
| [autokey](/ciphers/autokey) | polyalphabetic | `key` | ✗ |
| [trithemius](/ciphers/trithemius) | polyalphabetic | | ✗ |
| [alberti](/ciphers/alberti) | polyalphabetic | `key`, `period` | ✗ |
| [affine](/ciphers/affine) | multiplicative | `a` (default 5), `b` (default 8) | ✗ |
| [playfair](/ciphers/playfair) | digraph | `key` | ✗ |
| [polybius](/ciphers/polybius) | fractionation | `key` optional | ✗ |
| [morse](/ciphers/morse) | fractionation | | ✗ |
| [bacon](/ciphers/bacon) | fractionation | `letters` (24 or 26, default 26) | ✗ |
| [tap-code](/ciphers/tap-code) | fractionation | | ✗ |
| [adfgvx](/ciphers/adfgvx) | fractionation | `key` optional, `transposition` optional | ✗ |
| [bifid](/ciphers/bifid) | fractionation | `key` optional, `period` (default 5) | ✗ |
| [rail-fence](/ciphers/rail-fence) | transposition | `rails` (default 3) | ✗ |
| [columnar](/ciphers/columnar) | transposition | `key` | ✗ |
| [enigma](/ciphers/enigma) | rotor | `positions`, `rings`, `plugboard` | ✓ |
| [aes](/ciphers/aes) | substitution-permutation | `key` (32, 48 or 64 hex digits) | ✗ |
| [aes-cbc](/ciphers/aes-cbc) | substitution-permutation | `key` (32, 48 or 64 hex digits), `iv` (32 hex digits) | ✗ |
| [aes-cfb](/ciphers/aes-cfb) | substitution-permutation | `key` (32, 48 or 64 hex digits), `iv` (32 hex digits), `segment` (default 128) | ✗ |
| [aes-ofb](/ciphers/aes-ofb) | substitution-permutation | `key` (32, 48 or 64 hex digits), `iv` (32 hex digits) | ✗ |
| [aes-ctr](/ciphers/aes-ctr) | substitution-permutation | `key` (32, 48 or 64 hex digits), `iv` (32 hex digits, the first counter block) | ✗ |
| [aes-ccm](/ciphers/aes-ccm) | substitution-permutation | `key` (32, 48 or 64 hex digits), `nonce` (14 to 26 hex digits), `aad`, `tagLength` (default 128) | ✗ |
| [aes-ocb](/ciphers/aes-ocb) | substitution-permutation | `key` (32, 48 or 64 hex digits), `nonce` (2 to 30 hex digits), `aad`, `tagLength` (default 128) | ✗ |
| [aes-lrw](/ciphers/aes-lrw) | substitution-permutation | `key` (64, 80 or 96 hex digits), `tweak` (default 1) | ✗ |
| [aes-xts](/ciphers/aes-xts) | substitution-permutation | `key` (64 or 128 hex digits), `tweak` (default 0) | ✗ |
| [aes-cbc-mac](/ciphers/aes-cbc-mac) | substitution-permutation | `key` (32, 48 or 64 hex digits) | ✗ |
| [rijndael](/ciphers/rijndael) | substitution-permutation | `key` (32, 40, 48, 56 or 64 hex digits), `blockSize` (default 128) | ✗ |
| [des](/ciphers/des) | feistel | `key` (16 hex digits) | ✗ |
| [desx](/ciphers/desx) | feistel | `key` (48 hex digits) | ✗ |
| [triple-des](/ciphers/triple-des) | feistel | `key` (32 or 48 hex digits) | ✗ |
| [triple-des-cbc](/ciphers/triple-des-cbc) | feistel | `key` (32 or 48 hex digits), `iv` (16 hex digits) | ✗ |
| [blowfish](/ciphers/blowfish) | feistel | `key` (8 to 112 hex digits, an even number) | ✗ |
| [idea](/ciphers/idea) | lai-massey | `key` (32 hex digits) | ✗ |
| [lucifer](/ciphers/lucifer) | feistel | `key` (32 hex digits) | ✗ |
| [mars](/ciphers/mars) | feistel | `key` (32 to 112 hex digits, in steps of 8) | ✗ |

## Errors

```ts
import { CipherError, InvalidOptionError, MissingOptionError, UnknownCipherError } from "@agntn/ciphers";

try {
  create("caesar").encode("HELLO", { shift: 26 });
} catch (error) {
  if (error instanceof InvalidOptionError) {
    error.option; // "shift"
    error.reason; // why 26 is not a shift
  }
}
```

No key where a key is required - `MissingOptionError`. Name nobody registered - `UnknownCipherError`, and its message lists the names that are, so `rot-13` comes back with `rot13` in the same line. Value out of range - `InvalidOptionError` with the option, the value and the reason. Anything else a cipher throws gets wrapped by `normalizeError` into a `CipherError` that names the cipher. The CLI prints the message and exits 1. No stack trace, you did nothing wrong, the input did.

## Next

- [Encode and decode](/guide/transform): options, case, what happens to punctuation, the round trip.
- [Analysis](/guide/analysis): brute force a Caesar, read a histogram.
- [CLI](/guide/cli): the `ciphers` command.
- [Agents](/guide/agents): five tools over MCP, Pi and OMP.
- [Custom ciphers](/guide/custom): extend `Cipher`, register it.
- [Playground](/playground): every cipher, in the page.
